# Build tools pinned to avoid upstream packaging regressions while installing
# `openai-whisper` from source (Windows users reported `KeyError: '__version__'`).
setuptools==68.2.2
wheel==0.41.3

fastapi==0.110.0
uvicorn[standard]==0.27.1
python-dotenv==1.0.1
# Use the canonical package name and install from GitHub to avoid metadata
# inconsistencies on Windows/Python combinations (e.g., KeyError: '__version__').
openai-whisper @ git+https://github.com/openai/whisper.git@c0d2f624c09dc18e709e37c2ad90c039a4eb72a2#egg=openai-whisper
requests==2.31.0
huggingface-hub==0.23.5
python-multipart==0.0.9
